# 2026-03-24

## Day Summary

- Standardized ASM 17 community brand assets around PNG files and connected the social sharing metadata.
- Reorganized the app entry flow so `/seoul/dashboard` is the primary landing route and the previous home screen lives at `/home`.
- Rebuilt the creator persona from the latest Hupository data and upgraded it into a `Creator PR` page for team building.
- Unified the persona page around one card system, one color direction, SDGs, and a cleaner tech-stack layout.
- Refined navigation i18n, tech-stack naming, icons, and the overall wording across the creator profile.
- Rewrote the top-level GitHub README in Korean and added `README_eng.md` so the repo now explains the real ASM17 team-building problem, the current product flow, and the future AI matching idea more honestly.
- Rebuilt the `/home` page so it now reads like a product introduction driven by the README content instead of a small status screen, while keeping the backend health check as a playful mini widget.

## Recommended Reading Order

1. Branding and entry flow
2. Creator PR data and page purpose
3. Persona UI system
4. Navigation, i18n, and tech-stack polish
5. Roadmap and repo docs sync

## 1. Branding And Entry Flow

### What Changed

- Connected the `favicon`, `OG image`, and `apple-touch-icon` using PNG assets.
- Redirected `/` to `/seoul/dashboard` while keeping the previous landing page at `/home`.
- Simplified the sidebar header into a logo + tagline layout.

### Why It Mattered

- The current product now opens directly into the Seoul dashboard instead of a generic landing screen.
- Brand assets and social previews are more consistent across browser tabs and shared links.

### Files To Read

- [index.html](../../apps/frontend/index.html)
- [router.tsx](../../apps/frontend/src/router.tsx)
- [App.tsx](../../apps/frontend/src/App.tsx)

## 2. Creator PR Data And Page Purpose

### What Changed

- Rewrote the bilingual creator persona seed so Sejong now reads first as a Founder-PM with strong domain understanding, user understanding, community sense, documentation habits, and direction-setting ability.
- Added creator-only sections for the two target owner roles, the project direction, why now, why me, and a clearer CTA.
- Kept the existing visual system, but reordered the first screens so role, teammate fit, and project intent are visible before the deeper persona detail cards.

### Why It Mattered

- The creator page no longer reads like an old demo profile or a generic "nice person" introduction.
- It now works more like a recruiting surface that answers "What role does Sejong play?" and "Which two owners make this a strong team?" within the first minute.

### Files To Read

- [seed.py](../../apps/backend/app/common/seed.py)
- [persona-page.tsx](../../apps/frontend/src/features/persona/pages/persona-page.tsx)
- [persona.json](../../apps/frontend/public/locales/ko/persona.json)
- [persona.json](../../apps/frontend/public/locales/en/persona.json)

## 3. Persona UI Was Unified

### What Changed

- Unified the persona page into one consistent card system with shared typography, spacing, and inner panel structure.
- Softened the page colors and enlarged the SDGs badges so the whole page feels calmer and easier to read.
- Reworked the tech stack into a responsive grid with category-based ordering.

### Why It Mattered

- The page now feels like one product surface instead of several separately grown cards.
- Reading order is smoother on both mobile and desktop.

### Files To Read

- [persona-page.tsx](../../apps/frontend/src/features/persona/pages/persona-page.tsx)

## 4. Navigation, I18n, And Tech-Stack Polish

### What Changed

- Removed the separate `EN/KO` toggle from the persona page and unified language switching through the global navigation toggle.
- Moved the remaining sidebar labels fully into i18n so the entire nav now switches language consistently.
- Renamed the creator menu item to `Creator PR` to match the current purpose of the page.
- Removed `Kubernetes` and `Terraform`, then added `Docker`, `FastAPI`, `LangGraph`, `Vue.js`, `React Router v7`, `Redis`, `Ubuntu`, and `GitHub Actions`.
- Adjusted the categories for `Java` and `Redis`, and updated labels such as `LangGraph` and `AmazonWebServices`.
- Corrected the Ubuntu and LangGraph icons to better match the intended visual identity.

### Why It Mattered

- The language toggle now affects the full navigation instead of only part of the app.
- The creator stack reads more intentionally and better matches the tools that matter for current team building.

### Files To Read

- [App.tsx](../../apps/frontend/src/App.tsx)
- [common.json](../../apps/frontend/public/locales/ko/common.json)
- [common.json](../../apps/frontend/public/locales/en/common.json)
- [seed.py](../../apps/backend/app/common/seed.py)
- [langgraph-mark.svg](../../apps/frontend/public/langgraph-mark.svg)

## 5. Roadmap And Repo Docs Were Synchronized

### What Changed

- Updated the roadmap to reflect the `2026-03-24` completed work, including the new brand/creator-PR polish bundle.
- Added clearer next priorities around safety tools, reviewed-user management, commitment-window signals, and vision alignment for team building.
- Rewrote `README.md` as a Korean GitHub-facing product README and added `README_eng.md` as a matching English companion.
- Reframed the creator PR page in the README as one surface inside the product rather than the whole product.
- Reworked the `/home` page copy and information architecture so the product problem, current user flow, key features, and future direction are visible at a glance.
- Demoted the old backend health panel into a smaller interactive card so the home page still keeps a bit of engineering personality without overpowering the product story.
- Tightened the home-page copy again by reducing font scale, adding more color accents, inserting the `17th-cohort verification` step into the main flow, and compressing longer explanations into keyword-first summaries.
- Removed redundant home-page action buttons that duplicated the sidebar navigation, leaving the page more focused on product explanation than page-to-page jumping.
- Removed the extra “pages you can explore now” cards and the duplicated problem-intro box so the home page keeps only one explanation path for the product story.
- Removed the remaining hero summary cards (`problem`, `core info`, `current scope`) because they repeated the story already told in the sections below.
- Compressed the hero again into a shorter title plus a single why-it-exists sentence, and removed the separate `why` / `what users can do now` cards because the flow section already covered the same ground.
- Rephrased the home copy away from abstract `interview context` wording toward `people who interviewed together`, and shrank the backend widget into a smaller card that now shows measured health-check latency in milliseconds.
- Added a short trust/safety note on the home hero so visitors can see that member info is only shared among 17th-cohort-approved email accounts.
- Retuned the home accent colors so the backend poke button reads green, the trust/safety note reads red, and the `Notion` keyword chip stays white for cleaner contrast.
- Switched both the `current features` and `future direction` chips from loose wrap behavior to responsive grids so their widths adapt more evenly across mobile and desktop.
- Applied the same responsive width treatment to the hero keyword chips so the top summary labels now align more evenly as well.
- Updated the hero keyword set by removing `same-slot browsing` and adding `AI chatbot`, `17th-cohort chat room`, and `offline meetup` so the top summary better matches the broader community direction.
- Adjusted the small backend widget width rules so it now expands fully on narrow screens and keeps cleaner bounded widths across larger breakpoints.
- Added `Talk with AI Sejong` to the current-features list so the home summary now reflects the existing creator persona chat surface too.
- Reworded the future section so it now reads as `features being considered if enough members join` instead of sounding like the product depends on abstract `context accumulation`.
- Tightened the backend widget width rules again so `sm / md / lg / xl` each have explicit widths instead of relying only on `max-width` after `640px`.
- Simplified the backend widget sizing once more so it stays full-width through the single-column `640px~1023px` range and only switches to fixed widths again from `lg`.
- Removed the extra sidebar `open chat` text link and shortened the remaining contact CTA from `1:1 문의하기` to `1:1 문의`.
- Retuned the sidebar navigation hierarchy so `Dashboard - Seoul` now reads as the primary sky-blue entry, `Dashboard - Busan` looks more clearly disabled in gray, and `Creator PR` uses a softer red accent.
- Removed the extra `5개 분과 / 행 구성` and `점 5개 / 점 표시` cards from the applicant dashboard header so that area now stays focused on the filled-seat count and the actual board legend.
- Simplified the applicant dashboard access hint from `admins or approved users` wording down to the shorter `available after 17th-cohort verification`.
- Aligned the remaining signup-status card to sit more cleanly at the top-right of the applicant dashboard header instead of floating with the old multi-card layout rules.
- Replaced the generic slot-click access failure message with a clearer guide telling users to finish email signup verification and then 17th-cohort verification before viewing member details.
- Refined the small dashboard access badge once more so it now says `you can view 17th-cohort information after 17th-cohort verification`.
- Inserted a visible line break in the applicant dashboard intro copy so the seat-capacity sentence and the dot-board explanation read as two separate lines.
- Adjusted selected-slot colors on the applicant dashboard so Saturday selections now turn blue and Sunday selections turn red instead of both falling back to the default black selected state.
- Softened those selected-slot colors again so the default selection is now light green and the Saturday/Sunday blue-red tones feel less saturated and less heavy.
- Nudged those softened selection colors back up slightly so the chosen slot still reads more clearly without returning to the earlier heavy contrast.
- Matched the `1T~5T` row labels to that same selection language so active time-slot labels now use soft green by default and inherit the Saturday-blue / Sunday-red tones instead of falling back to black.
- Added a live workshop countdown card to the applicant dashboard header that counts down to the Seoul workshop opening on `2026-04-03 13:00 KST` in `Day / Hour / Min / Sec`.
- Tweaked the applicant dashboard header again so the workshop timestamp now shows `(금)` and the signup-status card reads as `current count / 300` instead of using the seat-grid denominator.
- Simplified the signup-status card text again by removing the `가입 현황` label and folding the meaning directly into the numerator/denominator display as `/300(명)`.
- Changed the dashboard count card to use the real `approved member` total instead of slot-filled seat count, and aligned the slot-detail display to a consistent 5-card limit with matching copy.
- Moved the dashboard legend chips (`1줄 = 1분과`, `남성`, `여성`, `미신청`) under the intro text so the top dashboard panel now reads as a cleaner 1-row, 2-column layout on large screens.
- Tightened the vertical padding and internal spacing in that top dashboard header once more so the single-row large-screen layout feels less airy and more balanced.
- Renamed the dashboard header title from `면접자 대시보드` to `서울 면접자 대시보드` so the page scope is explicit.
- Replaced the old bottom sticky slot-detail panel with a centered dialog and re-laid the 5 member cards so they sit in a cleaner centered row on larger screens.
- Reworked those slot-detail cards again by removing seat numbers, adding birth-year data, switching the card content order to `name → birth year → email → GitHub → Notion`, and appending a sixth same-sized `SoMa 17` logo card inside a responsive 2-row grid.
- Tightened the slot-detail card UI once more so the birth year now sits inline as `(1988)` beside the name and the mail / GitHub / Notion actions are icon buttons that open externally instead of text links with `바로가기`.
- Adjusted those slot-detail actions again into a vertical 3-row stack, changed the email action to copy-on-click, and kept missing GitHub / Notion links clearly disabled instead of clickable.
- Changed the slot-detail dialog grid again so mobile now stacks the cards in a single column before expanding to 2 and then 3 columns on wider screens.
- Removed the extra `currently displayed based on 5 people per section` helper line from the slot-detail dialog header so that modal now opens with a cleaner title-only top line.
- Refined the slot-detail name line again so it now keeps a visible space before the parenthesis and can show residence inline next to the birth year, like `이름 (1988 · 서울)`.
- Updated the extra logo card inside the slot-detail dialog so its caption now reads `asm 17` and `AI Software Maestro 17` instead of the older `SoMa 17 / Software Maestro` text.
- Added an unauthenticated / unapproved slot-preview mode that keeps the same dialog layout, shows one clickable Park Sejong demo card plus the ASM 17 logo card, and fills the remaining four cards with a locked `17기 인증 후 열람 가능` state.
- Simplified that preview state again by removing the extra `로그인하기` CTA from the dialog so unauthenticated users now only see the guidance message and the demo/locked-card layout.
- Simplified the preview state one more time by removing the explanatory access banner too, leaving the locked `17기 인증 후 열람 가능` cards to communicate the restriction on their own.
- Retuned those locked preview cards once more so they now use the same pink family as female participant cards instead of the earlier amber warning tone.
- Added a small `- 예시` marker beside the Park Sejong preview card name line so the demo card reads more clearly as sample data.
- Synced the Park Sejong demo seed with the real saved profile fields for `lifedesigner88@gmail.com`, including name, birth date, residence, phone, interview slot info, approved status, and profile links, so fresh DB rebuilds recreate the same demo account state.
- Relaxed the demo seed for `DEMO_USER_PHONE` again so the phone number is blank by default unless explicitly set in env, and existing users are no longer forced back to a hardcoded phone value on every startup.
- Added a tiny local-storage-backed `봤음` checkbox to the Park Sejong demo preview card so unauthenticated users can still mark the sample card as reviewed without needing a backend table yet.
- Simplified that demo checkbox UI again by removing the `봤음` label and pill background, leaving only a minimal bare checkbox beside the sample card name.
- Unified the slot-detail member-card surface into one neutral card style across genders and shifted the demo checkbox accent to green so the review signal is clearer without splitting the UI by male/female color.
- Adjusted that once more so only the checkbox stays green and unified, while actual member cards return to the earlier blue-for-male and pink-for-female color cues.
- Localized the full `Dashboard - Seoul` page into Korean and English by wiring the header, legend, countdown, slot dialog, demo cards, and dashboard API error strings into the nav language toggle through `common.json`.
- Moved the `17th-cohort verification required` access badge out of the legend-chip row and onto its own line underneath so the dashboard header reads more cleanly on wide screens too.
- Updated the shared `StatusPill` component with `nowrap` behavior so short English labels such as `D-Day` no longer split into two lines on the dashboard.
- Renamed the dashboard cohort pill in both languages so it now reads `AI Software Maestro 17th Cohort` / `AI 소프트웨어마에스트로 17기`.
- Added a real `member_checks` table keyed by `viewer_user_id + target_user_id`, surfaced `is_checked` in slot-member responses, and wired logged-in dashboard card checkboxes to persist in the DB while keeping the unauthenticated demo card on local storage.
- Trimmed the `sejong` Creator PR page into a shorter team-building surface by removing the prompt-like copy CTA, hiding the deeper persona-analysis sections for that profile, and rewriting the main Korean/English PR copy to sound more concise, less repetitive, and more growth-oriented.
- Restored the `AI Sejong` conversation feature on the Creator PR page as a smaller bottom Q&A card, and added a clear note that AI-generated answers may differ from Sejong's actual opinion.
- Brought the `MBTI` and `SDGs` cards back onto the `sejong` Creator PR page while keeping the other deeper analysis sections hidden.
- Moved the `MBTI` card up so it now sits directly under the main `Founder-PM` hero card on the `sejong` page.
- Moved the `Tech Stack` card directly under the top `real team-building PR` intro card on the `sejong` page so the practical build surface shows earlier without interrupting the main PR flow later.
- Moved the `SDGs` section up as well so it now sits directly under the `looking for teammates` block on the `sejong` page and supports the team-fit reading flow earlier.
- Shortened the top `sejong` intro copy again so the page now opens with a much denser `role + fit + quick judgment` message instead of two longer explanatory sentences.
- Turned the top `real team-building PR` intro into a richer two-column card by adding a direct `Hupository` link and a right-side note explaining that the page was generated with AI from Sejong's Hupository data, signed `with Codex GPT 5.4`.
- Strengthened that right-side `AI note` card again with a more visible sky-tinted surface and an explicit `AI-generated page` heading so visitors can immediately recognize that the page was produced with AI from Hupository data.
- Moved the `with Codex GPT 5.4` signature down to the bottom of that right-side note card so the top of the card stays focused on the `AI-generated` message first.
- Retuned the `Hupository` button on the top intro card to a sky-blue treatment so it now aligns more naturally with the surrounding AI note while still reading as a distinct external link.
- Added a small `✅` emoji to the `Hupository` button so it catches the eye a bit faster without changing the card structure again.
- Renamed the remaining `SW Maestro 17` wording on the `sejong` Creator PR surface and its seed badge/title to `AI SW Maestro 17` so the top intro and event tag stay consistent with the newer naming.
- Removed the newly restored `At a glance / 빠른 요약` card again and brought the `identity timeline` section back onto the `sejong` page instead, matching the earlier creator-page memory the user wanted.
- Moved the `contact` card below the `identity timeline` on the `sejong` page so the creator-story flow now reads through identity first and then contact second.
- Shifted the `SDGs` section down from right under `looking for teammates` to right under `what to build` on the `sejong` page so that mission alignment now reads after the project direction instead of before it.
- Moved the `looking for teammates` card down again so it now sits immediately above `contact`, letting the page end on `timeline → teammate fit → contact`.
- Renamed the SDG section label from `SDGs alignment / SDGs 정렬` to the softer `SDGs of interest / 관심있는 SDGs` wording.
- Updated the SDG AI note copy as well so it now talks about inferred `interest / 관심` instead of inferred `alignment / 정렬`.
- Pulled the `identity timeline` up to sit directly below `SDGs of interest / 관심있는 SDGs` on the `sejong` page, so that value interest and identity history now read as one connected block.
- Rewrote the `why now` copy in the `sejong` seed so it now argues for using the service actively before team building: share context early, start conversations sooner, and make the short offline decision window more productive.
- Compressed that `why now` section again into the shorter `빠른 소통의 필요성 / Why faster communication matters` framing, with tighter summary bullets focused on early context sharing and better use of the offline window.
- Simplified both `looking for teammates` role cards in the `sejong` seed by shortening the role summaries and trimming the bullets down to the most important ownership signals.
- Rebuilt the `AI Sejong` chat from a client-only single-turn box into a login-only multi-turn feature backed by a new `persona_chat_messages` table, so every stored question/answer pair now lives in the DB while only a recent slice is fed back into the model for efficiency.
- Expanded the persona chat grounding from the earlier shallow profile-only prompt to a richer prompt that now combines persona fields, creator-PR data, and selected `Hupository` snippets chosen by question keywords.
- Added a developer-facing `How it works` dialog, explicit logging notice, and persisted history loading to the compact `AI Sejong` card so visitors can inspect the chat mechanics without turning the page back into a long prompt dump.
- Moved that `How it works` dialog out to a true page-level overlay via portal rendering, and added a `session reset` action that clears only the visible chat window while keeping the stored DB history intact.
- Retuned the `How it works` trigger button to a clearer blue treatment so the explainer entry point stands out more on the compact `AI Sejong` card.
- Tightened the reset behavior again by tagging persona chat records with `session_id` and moving the reset flow to the backend, so a page refresh now loads only the newest chat session instead of reviving older history.
- Added lightweight chat-bubble markdown rendering for `**bold**` emphasis, so AI replies now show simple emphasis as styled text instead of raw markdown symbols.
- Extended that lightweight chat formatting again so `## heading` lines now render as small in-bubble section headers instead of raw markdown text.
- Expanded the lightweight heading formatter once more so `#`, `##`, and `###` now all render as gentle in-bubble section headers, with small emoji accents to make AI replies easier to scan.
- Added a backend rate limit for `AI Sejong` chat so each logged-in user can now ask at most `10 questions per hour`, with matching UI copy and a clearer in-chat limit message when the cap is reached.
- Tightened `AI Sejong` reply length control by adding explicit compact-length rules to the model prompt, lowering the token budget, and shaping long answers again on the backend before they are stored and sent to the frontend.
- Fixed the compact `AI Sejong` chat bubble layout so longer multi-line replies are no longer visually clipped: the Q&A card now allows visible overflow, message bubbles have a little more vertical space, and each rendered line is treated as a block instead of a fragile span/`<br>` mix.
- Relaxed the compact chat length guard again so `AI Sejong` can now answer with up to roughly `1,200 characters / 20 lines` before backend shaping kicks in, which should feel less abruptly cut off while still protecting the UI.
- Rewrote the root Korean README and aligned the English companion so the current AI behavior is now documented more honestly: login-only access, DB-backed message logging, recent-turn multi-turn context, Hupository grounding, and the fact that this AI layer is still something we want to improve with teammates.
- Updated `AGENTS.md` so the repo purpose, active backend domains, data-sensitivity notes, and changelog-writing rules now match the current SoMa Community direction.

### Why It Mattered

- The roadmap now matches the actual state of the product instead of stopping at the earlier pivot milestone.
- `README.md` now helps both potential users and developers understand the project quickly without reading it as a hackathon pitch or a purely personal PR page.
- The landing experience now matches the rewritten README better, so visitors understand the real ASM17 team-building problem before they see implementation details.
- The persona chat is now far easier to explain and operate responsibly because it is login-gated, persisted, and explicit about what data it uses and why the logs are stored.
- The repo front page now better matches the actual product state, especially for visitors arriving from team-building or developer context who want to understand how `AI Sejong` works and where collaboration is still needed.
- `README.md` and `AGENTS.md` no longer describe the repo as if it were still mainly `PersonaMirror`.
- Future work should be easier because the operating docs now point in the same direction.

### Files To Read

- [ROADMAP.md](../../docs/changelog/ROADMAP.md)
- [README.md](../../README.md)
- [README_eng.md](../../README_eng.md)
- [App.tsx](../../apps/frontend/src/App.tsx)
- [common.json](../../apps/frontend/public/locales/ko/common.json)
- [common.json](../../apps/frontend/public/locales/en/common.json)
- [persona-page.tsx](../../apps/frontend/src/features/persona/pages/persona-page.tsx)
- [api.ts](../../apps/frontend/src/features/persona/utils/api.ts)
- [types.ts](../../apps/frontend/src/features/persona/utils/types.ts)
- [router.py](../../apps/backend/app/features/persona/router.py)
- [service.py](../../apps/backend/app/features/persona/service.py)
- [models.py](../../apps/backend/app/features/persona/models.py)
- [test_persona.py](../../apps/backend/tests/test_persona.py)
- [README.md](../../README.md)
- [README_eng.md](../../README_eng.md)
- [AGENTS.md](../../AGENTS.md)

## End-of-Day Result

- The main outcome of this work is `cleaned ASM 17 community branding`, `Seoul dashboard as the default entry`, and `a stronger Creator PR page`.
- Most importantly, the creator page is no longer an old demo profile. It now works more clearly as a team-building PR page that makes the Founder-PM role, target teammate fit, and project picture easier to judge fast.
