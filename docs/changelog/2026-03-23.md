# 2026-03-23

## Day Summary
Three tracks closed: (1) Supabase replaces local Postgres — `docker compose up` now runs 3 services instead of 5; (2) production Docker images + GHCR CI pipeline established for deployment to `asm17.huposit.kr`; (3) admin seed crash-loop on restart fixed.

---

## 1. Supabase as Managed DB

### What Changed
- `apps/backend/.env` + `apps/ai-worker/.env`: `DATABASE_URL` switched to Supabase connection string (ap-northeast-2 pooler, port 6543)
- `docker-compose.yml`: removed `db` (Postgres) and `redis` services; removed hardcoded `DATABASE_URL` overrides from `backend` and `ai-worker` environment blocks
- `app/common/db.py`: added `prepare_threshold=None` for Supabase PgBouncer (transaction pooler does not support prepared statements)

### Why It Mattered
Local Postgres added friction for deployment — every environment needed its own volume and init. Supabase free tier handles the DB externally; `docker compose up` now starts 3 services instead of 5. pgvector is available on Supabase when needed for persona matching (M3).

### Files To Read
- [apps/backend/app/common/db.py](../../apps/backend/app/common/db.py)
- [docker-compose.yml](../../docker-compose.yml)

---

## 2. Production Docker Images + GHCR CI

### What Changed
- `apps/frontend/Dockerfile`: converted to multi-stage build — `node:24.11.0-alpine` builder runs `pnpm build`, output copied into `nginx:1.27-alpine`; `VITE_API_BASE_URL` passed as build arg
- `apps/frontend/nginx.conf`: added `try_files $uri $uri/ /index.html` for SPA routing
- `.github/workflows/build-images.yml`: on push to `main`, builds and pushes `persona-mirror-frontend`, `persona-mirror-backend`, `persona-mirror-ai-worker` to GHCR with layer caching

### Why It Mattered
The old Dockerfile ran `pnpm dev` — a dev server not suitable for production. Multi-stage build produces a lightweight nginx image with baked-in static assets. `VITE_API_BASE_URL` is baked at build time so the browser knows where to reach the backend (`https://asm17.huposit.kr/api`).

### Files To Read
- [apps/frontend/Dockerfile](../../apps/frontend/Dockerfile)
- [apps/frontend/nginx.conf](../../apps/frontend/nginx.conf)
- [.github/workflows/build-images.yml](../../.github/workflows/build-images.yml)

---

## 3. Fix: Admin Seed Crash on Restart (Duplicate Email)

### What Changed
- `sync_admin_seed` in `auth/service.py`: existence check changed from `WHERE user_id = ?` to `WHERE user_id = ? OR email = ?`

### Why It Mattered
When `ADMIN_SEED_USER_ID` env var changed between deploys (e.g. `"admin"` → `"ADM001"`), the old row was not found by `user_id`, triggering an unconditional INSERT that violated the unique constraint on `email`. Backend crash-looped on every restart against Supabase. Checking by email as well makes the seed idempotent regardless of `user_id` changes.

### Files To Read
- [apps/backend/app/features/auth/service.py](../../apps/backend/app/features/auth/service.py)

---

## 4. `POST /persona/:id/ask` — Q&A Panel End-to-End

### Representative Commits
- `5bcffe9` feat: POST /persona/:id/ask Q&A panel endpoint, end-to-end

### What Changed
- `app/features/persona/schemas.py`: `AskRequest` (question, 1–500 chars) + `AskResponse` (answer)
- `app/features/persona/service.py`: `ask_persona()` builds a first-person system prompt from persona data fields (archetype, values, strengths, goals, MBTI) and calls Claude Haiku
- `app/features/persona/router.py`: `POST /{persona_id}/ask` — public endpoint, 404 if persona not found
- `tests/test_persona.py`: 5 tests — GET happy path, ask happy path (mocked Claude), 404 for both, empty question 422
- `apps/frontend/src/features/persona/utils/loader.ts`: demo route now fetches real backend persona (`d31sf2`) first; falls back to hardcoded `DEMO_PROFILE` if backend unreachable, so the Ask panel always has a valid `persona_id`
- `apps/frontend/src/features/persona/utils/api.ts`: removed stale TODO comment
- `apps/frontend/.env`: ports aligned to 3002 / 8002

### Why It Mattered
The frontend `PersonaQAPanel` was already wired to call `POST /persona/:id/ask` and render the response — the backend was the missing piece. The `/persona/demo` route had a subtler bug: it used `person_id: "demo"` from the hardcoded profile, causing Ask to POST to `/persona/demo/ask` (404). Fetching the real backend persona (`d31sf2`) at load time fixes this without changing the public URL.

### Files To Read
- [apps/backend/app/features/persona/service.py](../../apps/backend/app/features/persona/service.py)
- [apps/backend/app/features/persona/router.py](../../apps/backend/app/features/persona/router.py)
- [apps/frontend/src/features/persona/utils/loader.ts](../../apps/frontend/src/features/persona/utils/loader.ts)

---

## 5. Fix: Resend `RESEND_FROM` — Switch to Verified Domain

### Representative Commits
- `fix: RESEND_FROM → noreply@sejongclass.kr (verified domain)`

### What Changed
- `apps/backend/.env.example`: `RESEND_FROM` changed from `onboarding@resend.dev` to `noreply@sejongclass.kr`

### Why It Mattered
Resend's shared test domain (`onboarding@resend.dev`) restricts delivery to the account owner's email only (`lifedesigner88@gmail.com`). All other recipients received a 403 `validation_error`. `sejongclass.kr` is already verified in the Resend dashboard, so switching the sender domain removes the restriction and allows delivery to any address.

### Files To Read
- [apps/backend/.env.example](../../apps/backend/.env.example)

---

## End-of-Day Result

### Working
- `pnpm dev` — all 3 app services on host, Supabase as DB
- `pnpm docker` — all 3 app services as Nx-built containers, Supabase as DB
- `docker compose up` — 3 services (frontend, backend, ai-worker); no local DB needed
- GHCR CI — images pushed on every push to `main`
- `/persona/demo` Ask panel — logged-in users can ask questions; Claude answers in-persona

### Not Yet Done
- Voice and image capture
